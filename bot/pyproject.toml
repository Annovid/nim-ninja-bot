[project]
name = "nim-ninja-bot"
version = "0.2.0"
description = "Telegram bot that launches the Nim Mini App"
requires-python = ">=3.11"
dependencies = [
    "pyTelegramBotAPI>=4.21.0",
    "python-dotenv>=1.0.0",
]
